{
    "forEach": "apps",
    "filename": "packages/app-{{kebabCase name}}/zip.js",
    "formatUsing": "babel"
}
---
const { zip } = require("zip-a-folder");
const path = require("path");

console.log(`Creating zip for {{kebabCase name}} ...`);
zip(path.join(__dirname, "./public"), path.join(__dirname, "./{{kebabCase name}}.zip"));
console.log(`Zip created for {{kebabCase name}}`);

